import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const ProfileSelfBottomSheet = () => {
    return (
        <View>
            <Text></Text>
        </View>
    );
};

export default ProfileSelfBottomSheet;

const styles = StyleSheet.create({});
